router id 192.168.16.100;
protocol device {
        debug all;
        scan time 10;
}
protocol direct {
        debug all;
}
protocol kernel {
        debug all;
        persist no;
        kernel table 254;
        learn;
        scan time 10;
        import all;
        export all;
}
